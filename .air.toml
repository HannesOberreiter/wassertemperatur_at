root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/main"
  cmd = "go build -o ./tmp/main ."
  delay = 1000
  exclude_dir = ["tmp", "db", "vendor"]
  include_ext = ["go", "html", "css"]
  send_interrupt = true
  kill_delay = "1s"
  stop_on_error = true

[log]
  time = false
